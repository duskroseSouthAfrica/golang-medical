<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Patient Record Management System</title>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            padding: 20px;
            color: #333;
        }

        .header {
            text-align: center;
            margin-bottom: 30px;
            color: white;
        }

        .header h1 {
            font-size: 2.5rem;
            margin-bottom: 10px;
            text-shadow: 0 2px 4px rgba(0,0,0,0.3);
        }

        .header p {
            font-size: 1.1rem;
            opacity: 0.9;
        }

        .container {
            background: white;
            border-radius: 20px;
            box-shadow: 0 20px 60px rgba(0,0,0,0.1);
            max-width: 1200px;
            margin: 0 auto;
            overflow: hidden;
        }

        .form-header {
            background: linear-gradient(135deg, #2c3e50, #34495e);
            color: white;
            padding: 30px;
            text-align: center;
        }

        .form-header h2 {
            font-size: 2rem;
            margin-bottom: 10px;
        }

        .form-header p {
            opacity: 0.9;
            font-size: 1rem;
        }

        .form-content {
            padding: 40px;
        }

        .section {
            margin-bottom: 40px;
            border: none;
            padding: 0;
        }

        .section-header {
            display: flex;
            align-items: center;
            margin-bottom: 25px;
            padding-bottom: 15px;
            border-bottom: 3px solid #e74c3c;
            background: linear-gradient(90deg, #e74c3c, #c0392b);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        .section-header i {
            font-size: 1.5rem;
            margin-right: 15px;
            color: #e74c3c;
            -webkit-text-fill-color: #e74c3c;
        }

        .section-header h3 {
            font-size: 1.4rem;
            font-weight: 600;
        }

        .form-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
            gap: 20px;
            margin-bottom: 30px;
        }

        .form-group {
            position: relative;
        }

        .form-group.full-width {
            grid-column: 1 / -1;
        }

        .form-group.half-width {
            grid-column: span 2;
        }

        .form-control {
            width: 100%;
            padding: 15px 20px;
            border: 2px solid #e0e6ed;
            border-radius: 10px;
            font-size: 16px;
            background: #fff;
            transition: all 0.3s ease;
            font-family: inherit;
        }

        .form-control:focus {
            outline: none;
            border-color: #3498db;
            box-shadow: 0 0 0 3px rgba(52, 152, 219, 0.1);
            transform: translateY(-2px);
        }

        .form-control::placeholder {
            color: #a0a0a0;
            font-weight: 400;
        }

        textarea.form-control {
            min-height: 100px;
            resize: vertical;
            font-family: inherit;
        }

        .input-icon {
            position: absolute;
            left: 15px;
            top: 50%;
            transform: translateY(-50%);
            color: #a0a0a0;
            pointer-events: none;
            z-index: 2;
        }

        .form-control.with-icon {
            padding-left: 50px;
        }

        .required {
            color: #e74c3c;
            margin-left: 3px;
        }

        .form-row {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 20px;
        }

        .form-row.triple {
            grid-template-columns: 1fr 1fr 1fr;
        }

        .submit-section {
            background: linear-gradient(135deg, #f8f9fa, #e9ecef);
            padding: 30px;
            border-radius: 15px;
            text-align: center;
            margin-top: 30px;
        }

        .submit-btn {
            background: linear-gradient(135deg, #27ae60, #2ecc71);
            color: white;
            border: none;
            padding: 15px 40px;
            font-size: 18px;
            font-weight: 600;
            border-radius: 50px;
            cursor: pointer;
            transition: all 0.3s ease;
            box-shadow: 0 5px 15px rgba(46, 204, 113, 0.3);
            min-width: 200px;
        }

        .submit-btn:hover {
            transform: translateY(-3px);
            box-shadow: 0 8px 25px rgba(46, 204, 113, 0.4);
            background: linear-gradient(135deg, #229954, #27ae60);
        }

        .submit-btn:active {
            transform: translateY(-1px);
        }

        .submit-btn i {
            margin-right: 10px;
        }

        @media (max-width: 768px) {
            .form-content {
                padding: 20px;
            }
            
            .form-grid {
                grid-template-columns: 1fr;
            }
            
            .form-row,
            .form-row.triple {
                grid-template-columns: 1fr;
            }
            
            .header h1 {
                font-size: 2rem;
            }
            
            .form-header h2 {
                font-size: 1.5rem;
            }
        }

        .clinical-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
            gap: 20px;
        }

        .info-card {
            background: linear-gradient(135deg, #f8f9fa, #e9ecef);
            padding: 20px;
            border-radius: 15px;
            border-left: 5px solid #3498db;
            margin-bottom: 20px;
        }

        .info-card h4 {
            color: #2c3e50;
            margin-bottom: 10px;
            display: flex;
            align-items: center;
        }

        .info-card h4 i {
            margin-right: 10px;
            color: #3498db;
        }

        .progress-indicator {
            display: flex;
            justify-content: space-between;
            margin-bottom: 30px;
            padding: 0 20px;
        }

        .progress-step {
            display: flex;
            flex-direction: column;
            align-items: center;
            flex: 1;
            position: relative;
        }

        .progress-step::after {
            content: '';
            position: absolute;
            top: 15px;
            left: 50%;
            width: 100%;
            height: 2px;
            background: #e0e6ed;
            z-index: 1;
        }

        .progress-step:last-child::after {
            display: none;
        }

        .step-circle {
            width: 30px;
            height: 30px;
            border-radius: 50%;
            background: #3498db;
            color: white;
            display: flex;
            align-items: center;
            justify-content: center;
            font-weight: bold;
            z-index: 2;
            position: relative;
        }

        .step-label {
            margin-top: 10px;
            font-size: 12px;
            color: #666;
            text-align: center;
        }
    </style>
</head>
<body>
    <div class="header">
        <h1><i class="fas fa-hospital"></i> MedRecord Pro</h1>
        <p>Professional Patient Record Management System</p>
    </div>

    <div class="container">
        <div class="form-header">
            <h2><i class="fas fa-user-md"></i> Patient Record Form</h2>
            <p>Complete patient information and medical records</p>
        </div>

        <div class="form-content">
            <div class="progress-indicator">
                <div class="progress-step">
                    <div class="step-circle">1</div>
                    <div class="step-label">Patient Info</div>
                </div>
                <div class="progress-step">
                    <div class="step-circle">2</div>
                    <div class="step-label">Medical Data</div>
                </div>
                <div class="progress-step">
                    <div class="step-circle">3</div>
                    <div class="step-label">Complete</div>
                </div>
            </div>

            <form action="/finish" method="post">
                <div class="section">
                    <div class="section-header">
                        <i class="fas fa-user"></i>
                        <h3>Patient Demographics & Contact Information</h3>
                    </div>
                    
                    <div class="info-card">
                        <h4><i class="fas fa-info-circle"></i> Basic Information</h4>
                        <div class="form-row triple">
                            <div class="form-group">
                                <i class="fas fa-id-card input-icon"></i>
                                <input type="text" name="unique_id" class="form-control with-icon" placeholder="Unique Patient ID" required>
                            </div>
                            <div class="form-group">
                                <i class="fas fa-user input-icon"></i>
                                <input type="text" name="first_name" class="form-control with-icon" placeholder="First Name" required>
                            </div>
                            <div class="form-group">
                                <i class="fas fa-user input-icon"></i>
                                <input type="text" name="middle_name" class="form-control with-icon" placeholder="Middle Name">
                            </div>
                        </div>
                        <div class="form-row">
                            <div class="form-group">
                                <i class="fas fa-user input-icon"></i>
                                <input type="text" name="surname" class="form-control with-icon" placeholder="Surname" required>
                            </div>
                            <div class="form-group">
                                <i class="fas fa-calendar input-icon"></i>
                                <input type="date" name="dob" class="form-control with-icon" required>
                            </div>
                        </div>
                        <div class="form-row">
                            <div class="form-group">
                                <i class="fas fa-venus-mars input-icon"></i>
                                <select name="gender" class="form-control with-icon" required>
                                    <option value="">Select Gender</option>
                                    <option value="male">Male</option>
                                    <option value="female">Female</option>
                                    <option value="other">Other</option>
                                    <option value="prefer-not-to-say">Prefer not to say</option>
                                </select>
                            </div>
                            <div class="form-group">
                                <i class="fas fa-heart input-icon"></i>
                                <select name="marital_status" class="form-control with-icon">
                                    <option value="">Marital Status</option>
                                    <option value="single">Single</option>
                                    <option value="married">Married</option>
                                    <option value="divorced">Divorced</option>
                                    <option value="widowed">Widowed</option>
                                </select>
                            </div>
                        </div>
                    </div>

                    <div class="info-card">
                        <h4><i class="fas fa-address-book"></i> Contact Details</h4>
                        <div class="form-row">
                            <div class="form-group">
                                <i class="fas fa-phone input-icon"></i>
                                <input type="tel" name="phone" class="form-control with-icon" placeholder="Phone Number" required>
                            </div>
                            <div class="form-group">
                                <i class="fas fa-envelope input-icon"></i>
                                <input type="email" name="email" class="form-control with-icon" placeholder="Email Address">
                            </div>
                        </div>
                        <div class="form-group">
                            <i class="fas fa-map-marker-alt input-icon"></i>
                            <textarea name="address" class="form-control with-icon" placeholder="Physical Address" style="padding-left: 50px;"></textarea>
                        </div>
                    </div>

                    <div class="info-card">
                        <h4><i class="fas fa-users"></i> Emergency Contact</h4>
                        <div class="form-row triple">
                            <div class="form-group">
                                <i class="fas fa-user-friends input-icon"></i>
                                <input type="text" name="next_of_kin" class="form-control with-icon" placeholder="Next of Kin Name">
                            </div>
                            <div class="form-group">
                                <i class="fas fa-link input-icon"></i>
                                <input type="text" name="relationship" class="form-control with-icon" placeholder="Relationship">
                            </div>
                            <div class="form-group">
                                <i class="fas fa-phone input-icon"></i>
                                <input type="tel" name="kin_contact" class="form-control with-icon" placeholder="Emergency Contact">
                            </div>
                        </div>
                    </div>

                    <div class="info-card">
                        <h4><i class="fas fa-shield-alt"></i> Insurance Information</h4>
                        <div class="form-row">
                            <div class="form-group">
                                <i class="fas fa-building input-icon"></i>
                                <input type="text" name="insurance_provider" class="form-control with-icon" placeholder="Medical Aid/Insurance Provider">
                            </div>
                            <div class="form-group">
                                <i class="fas fa-id-badge input-icon"></i>
                                <input type="text" name="member_number" class="form-control with-icon" placeholder="Member/Policy Number">
                            </div>
                        </div>
                    </div>
                </div>

                <div class="section">
                    <div class="section-header">
                        <i class="fas fa-stethoscope"></i>
                        <h3>Clinical & Medical Information</h3>
                    </div>

                    <div class="clinical-grid">
                        <div class="form-group">
                            <label style="display: block; margin-bottom: 8px; font-weight: 600; color: #2c3e50;">
                                <i class="fas fa-history"></i> Clinical History
                            </label>
                            <textarea name="clinical_history" class="form-control" placeholder="Patient's clinical history, past medical conditions, surgeries, etc." rows="4"></textarea>
                        </div>

                        <div class="form-group">
                            <label style="display: block; margin-bottom: 8px; font-weight: 600; color: #e74c3c;">
                                <i class="fas fa-exclamation-triangle"></i> Allergies & Adverse Reactions
                            </label>
                            <textarea name="allergies" class="form-control" placeholder="Known allergies, drug reactions, food intolerances" rows="4"></textarea>
                        </div>

                        <div class="form-group">
                            <label style="display: block; margin-bottom: 8px; font-weight: 600; color: #2c3e50;">
                                <i class="fas fa-diagnoses"></i> Medical Assessments
                            </label>
                            <textarea name="assessments" class="form-control" placeholder="Current diagnosis, medical assessments, examination findings" rows="4"></textarea>
                        </div>

                        <div class="form-group">
                            <label style="display: block; margin-bottom: 8px; font-weight: 600; color: #27ae60;">
                                <i class="fas fa-clipboard-list"></i> Treatment & Management Plan
                            </label>
                            <textarea name="treatment_plan" class="form-control" placeholder="Treatment protocols, management strategies, care plan" rows="4"></textarea>
                        </div>

                        <div class="form-group">
                            <label style="display: block; margin-bottom: 8px; font-weight: 600; color: #8e44ad;">
                                <i class="fas fa-pills"></i> Current Medications
                            </label>
                            <textarea name="medication" class="form-control" placeholder="Current medications, dosages, frequency, duration" rows="4"></textarea>
                        </div>

                        <div class="form-group">
                            <label style="display: block; margin-bottom: 8px; font-weight: 600; color: #f39c12;">
                                <i class="fas fa-user-md"></i> Specialist Referrals
                            </label>
                            <textarea name="referrals" class="form-control" placeholder="Referrals to specialists, recommended consultations" rows="4"></textarea>
                        </div>
                    </div>

                    <div class="info-card" style="margin-top: 30px;">
                        <h4><i class="fas fa-flask"></i> Test Results & Investigations</h4>
                        <div class="form-group">
                            <textarea name="test_results" class="form-control" placeholder="Laboratory results, imaging studies, diagnostic test outcomes" rows="4"></textarea>
                        </div>
                    </div>

                    <div class="info-card">
                        <h4><i class="fas fa-calendar-check"></i> Consultation Details</h4>
                        <div class="form-row">
                            <div class="form-group">
                                <i class="fas fa-clock input-icon"></i>
                                <input type="datetime-local" name="consultation_datetime" class="form-control with-icon">
                            </div>
                            <div class="form-group">
                                <i class="fas fa-map-marker-alt input-icon"></i>
                                <input type="text" name="consultation_place" class="form-control with-icon" placeholder="Consultation Location/Facility">
                            </div>
                        </div>
                        <div class="form-group">
                            <label style="display: block; margin-bottom: 8px; font-weight: 600; color: #2c3e50;">
                                <i class="fas fa-comment-medical"></i> Patient Response to Treatment
                            </label>
                            <textarea name="patient_reaction" class="form-control" placeholder="Patient's response to treatment, side effects, compliance, follow-up notes" rows="3"></textarea>
                        </div>
                    </div>
                </div>

                <div class="submit-section">
                    <button type="submit" class="submit-btn">
                        <i class="fas fa-save"></i>
                        Save Patient Record
                    </button>
                    <p style="margin-top: 15px; color: #666; font-size: 14px;">
                        <i class="fas fa-lock"></i> All patient information is securely encrypted and HIPAA compliant
                    </p>
                </div>
            </form>
        </div>
    </div>

    <script>
        // Add some interactivity
        document.addEventListener('DOMContentLoaded', function() {
            const form = document.querySelector('form');
            const inputs = document.querySelectorAll('.form-control');
            
            // Add focus animations
            inputs.forEach(input => {
                input.addEventListener('focus', function() {
                    this.parentElement.style.transform = 'scale(1.02)';
                });
                
                input.addEventListener('blur', function() {
                    this.parentElement.style.transform = 'scale(1)';
                });
            });
            
            // Form validation feedback
            form.addEventListener('submit', function(e) {
                const requiredFields = document.querySelectorAll('[required]');
                let isValid = true;
                
                requiredFields.forEach(field => {
                    if (!field.value.trim()) {
                        field.style.borderColor = '#e74c3c';
                        field.style.boxShadow = '0 0 0 3px rgba(231, 76, 60, 0.1)';
                        isValid = false;
                    } else {
                        field.style.borderColor = '#27ae60';
                        field.style.boxShadow = '0 0 0 3px rgba(39, 174, 96, 0.1)';
                    }
                });
                
                if (!isValid) {
                    e.preventDefault();
                    alert('Please fill in all required fields.');
                }
            });
        });
    </script>
</body>
</html>